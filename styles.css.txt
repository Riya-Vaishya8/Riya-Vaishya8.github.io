/* simple modern gradient theme + responsive CSS */
* { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }
body {
  font-family: "Poppins", sans-serif;
  color: #fff;
  background: linear-gradient(135deg,#2b1055 0%,#7f5af0 50%,#1dbde6 100%);
  min-height: 100vh;
}

/* NAV */
.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 18px 6%;
  background: rgba(255,255,255,0.06);
  position: sticky;
  top: 0;
  backdrop-filter: blur(6px);
}
.logo { font-weight: 700; font-size: 1.2rem; }
.nav-links { list-style: none; display: flex; gap: 20px; }
.nav-links a { color: #fff; text-decoration: none; font-weight: 500; }
.menu-toggle { display: none; cursor: pointer; font-size: 1.4rem; }

/* HERO */
.hero {
  display: flex;
  gap: 40px;
  align-items: center;
  justify-content: space-between;
  padding: 80px 6%;
}
.hero-text h1 { font-size: 2.6rem; margin-bottom: 12px; }
.hero-text p { opacity: 0.95; margin-bottom: 18px; }
.cta {
  background: #fff;
  color: #222;
  padding: 10px 18px;
  border-radius: 8px;
  text-decoration: none;
  font-weight: 600;
}

/* Illustration */
.hero-illustration img { width: 380px; max-width: 100%; border-radius: 12px; }

/* Projects */
.projects { padding: 60px 6%; }
.grid { display: flex; gap: 20px; flex-wrap: wrap; justify-content: center; }
.card {
  background: rgba(255,255,255,0.08);
  padding: 18px;
  border-radius: 12px;
  width: 260px;
  backdrop-filter: blur(6px);
  transition: transform .25s, box-shadow .25s;
}
.card:hover { transform: translateY(-8px); box-shadow: 0 8px 30px rgba(0,0,0,0.2); }

/* About & Footer */
.about { padding: 60px 6%; text-align: center; }
.footer { text-align: center; padding: 18px; opacity: 0.9 }

/* MEDIA QUERIES (mobile friendly) */
@media (max-width: 768px) {
  .nav-links { display: none; flex-direction: column; gap: 12px; background: rgba(0,0,0,0.3); padding: 14px; position: absolute; right:6%; top:60px; border-radius: 10px; }
  .menu-toggle { display: block; }
  .hero { flex-direction: column; text-align: center; padding: 48px 6%; gap: 24px; }
  .hero-illustration img { width: 85%; animation: float 3s ease-in-out infinite; }
  .grid { flex-direction: column; align-items: center; }
  .card { width: 100%; max-width: 420px; }
  .hero-text h1 { font-size: 1.8rem; }
}

/* small floating animation for hero image on mobile */
@keyframes float {
  0% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
  100% { transform: translateY(0); }
}
